# Pulse nightly builds

This repository contains nightly builds of Pulse in the releases page.
Each builds includes a working F*, the packages should be standalone.
A tag (and release) is pushed every night.
